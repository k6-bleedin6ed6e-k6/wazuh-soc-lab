[defaults]
inventory           = inventory/hosts.ini
private_key_file    = ~/.ssh/py-bite.pem
remote_user         = ubuntu
host_key_checking   = False
stdout_callback     = ansible.builtin.default
result_format       = yaml
become_timeout      = 120

[ssh_connection]
pipelining          = True
